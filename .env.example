# ── Required ──────────────────────────────────────────────────────────────────
# Random secret used to sign Payload CMS sessions and tokens.
# Generate with: openssl rand -base64 32
PAYLOAD_SECRET=change-me-use-openssl-rand-base64-32

# Public URL of your site (used by Payload live-preview and internal links).
# Must match your actual domain — it is compiled into the client bundle.
NEXT_PUBLIC_SITE_URL=https://yourdomain.com

# ── Database ──────────────────────────────────────────────────────────────────
# Path inside the container. Leave as-is when using the Docker volume.
DATABASE_URI=file:/data/payload.db

# ── Optional ──────────────────────────────────────────────────────────────────
# Port the Next.js server listens on inside the container (default: 3000)
# PORT=3000
