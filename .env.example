# ── .env  (Docker Compose — copy to .env) ────────────────────────────────────
DIRECTUS_SECRET=replace-with-a-strong-random-secret
DIRECTUS_ADMIN_EMAIL=admin@example.com
DIRECTUS_ADMIN_PASSWORD=change-me
DIRECTUS_PUBLIC_URL=http://localhost:8055   # or your production domain

# ── .env.local  (Next.js — copy to .env.local) ────────────────────────────────
# URL of your Directus instance (local or production).
DIRECTUS_URL=http://localhost:8055
# Optional: static access token (Settings → Access Tokens in Directus admin).
# Leave empty to rely on the Public access policy.
DIRECTUS_TOKEN=
